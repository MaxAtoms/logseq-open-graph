{
  "name": "logseq-open-graph",
  "version": "0.1.0",
  "main": "index.html",
  "logseq": {
    "id": "_rofpb28pg"
  },
  "description": "A plugin for Logseq which adds a slash command for capturing Open Graph information from a URL.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Atoms",
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "^0.0.14"
  },
  "devDependencies": {
    "prettier": "2.8.3"
  }
}
